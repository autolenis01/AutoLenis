-- Supabase seed file for AutoLenis
-- This file is executed once when a new local dev environment is created
-- via `supabase db reset`.  It should only contain reference/static data —
-- user records, test data, and real production rows belong in migration files.

-- Placeholder: add INSERT statements for look-up tables (e.g. fee tiers,
-- status codes, supported lenders) as the platform grows.
SELECT 1; -- no-op to keep the file valid SQL
